# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
/artifacts/
dist-ssr
*.local

# Rust / Tauri
src-tauri/target

# cfb sidecars are generated locally or downloaded from GitHub Releases
src-tauri/binaries/cfb-*
!src-tauri/binaries/.gitkeep

# 本地客户端烧录验证产物
.chis-verify.json
.zindex-verify/
_cmd_out/

# SkyEmu：安装路径由用户选择并记在 localStorage，勿提交二进制/测试目录
.tmp-skyemu*
SkyEmu*.exe
SkyEmu*.dmg
SkyEmu*.AppImage
SkyEmu*.zip

# 本地密钥（若有）
.env
.env.*
!.env.example
!.env.temporary.prod.example

.claude/settings.local.json
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

local-paths.json
# 运行时本地状态在浏览器 localStorage（chis.local.v1），无仓库根文件；勿新增可提交的本地状态文件。
.tauri/

# Cursor / agent 本地技能缓存，勿提交
.agents/
.cursor/

# NSIS 安装包产物（npm run build 生成，勿提交）
*setup.exe

# Vite 临时输出
.tmp-*
